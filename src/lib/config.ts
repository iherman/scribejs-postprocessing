
/**
 * ## Configuration settings
 *
 * @packageDocumentation
*/

/**
 * Directory collecting the local configuration files
 */
export const LOCAL_CONFIGS  = '/Users/ivan/W3C/github/Tools/scribejs/BrowserView/Groups/postprocessing/';

/**
 * (Github) URL where the configuration files are available locally
 */
export const GITHUB_CONFIGS = 'https://w3c.github.io/scribejs/BrowserView/Groups/postprocessing/';

/**
 * The name of the configuration file in the user’s home directory. It contains the user’s github API token (`ghtoken`), 
 * necessary for committing the results on the github repository.
 */
export const USER_CONFIG_NAME = ".ghid.json";

/** @internal */
export const DO_DEBUG  = false;

/** @internal */
export const DO_LOG    = true;

/** @internal */
export const DO_UPDATE = true;

/* ------------------------------------------------------------------------------------------------- */

